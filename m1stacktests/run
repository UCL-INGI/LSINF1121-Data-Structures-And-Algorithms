#!/usr/bin/env python

from subprocess import Popen, PIPE, STDOUT
from shutil import copyfile
from os import chdir
from ingiapi import feedback_and_exit, feedback_grade, feedback_append_msg, OVERFLOW, SUCCESS, FAILED, TIMEOUT, CRASH

feedback_and_exit(problemId, "Test", SUCCESS)
