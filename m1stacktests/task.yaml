accessible: true
author: Simon Hardy
context: |-
    Recall the Stack interface :

    .. code-block:: java

        public interface Stack<E> {

            public boolean empty();

            public E peek() throws EmptyStackException;

            public E pop() throws EmptyStackException;

            public E push(E item);

        }

    You are asked to write unit tests (using JUnit) in order to check wether a particular implementation of this interface is correct.

    Here is a simple template you can use to write your tests (you can use other types than String, of course!) :

    .. code-block:: java

        import org.junit.Test;
        import static org.junit.Assert.assertEquals;
        public class StackTests {

            String element = "a";
            MyStack<String> stack = new MyStack<String>();

            @Test
            public void firstTest() {
                stack.push(element);
                assertEquals(element,stack.pop());
            }

            @Test
            public void secondTest() {
                ...
            }
        }

    NB : to compile this, either use a built-in tool like Eclipse, or run the following Bash commands (download the JARs here https://github.com/junit-team/junit/wiki/Download-and-Install):

    .. code-block::

        javac MyStack.java
        javac -cp .:junit-4.12.jar StackTests.java
        java -cp .:junit-4.12.jar:hamcrest-core-1.3.jar org.junit.runner.JUnitCore StackTests
environment: java8scala
groups: false
limits:
    memory: '512'
    output: '100'
    time: '45'
name: Mission 1 - Unit tests for the stack [individual]
order: 1
problems:
    stack_tests:
        name: Unit tests for the stack implementation
        type: code-file
        header: Upload your file here, with only one class named "StackTests". The
            class representing the stack under test is called *MyStack*, as written
            in the template !
        allowed_exts:
        - .java
weight: 1.0
